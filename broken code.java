ArrayList<String> lettersArray = new ArrayList<String>();
lettersArray.add(".*[abcdefghijklmnopqrstuvwxyz].*");
int letterCounter = 0;
for (String letters : lettersArray) {
    if (passwordString.indexOf(String.valueOf(lettersArray)) == passwordString.indexOf(passwordString.substring() ) {
        passwordQualityLength = true;
    } else {
        passwordQualityTwoLettersInARow = false;
    }
}